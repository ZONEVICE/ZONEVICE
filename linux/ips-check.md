Tired of getting into the router to see the IP addresses of
 whatever is connected to WiFi. This solves it almost good
 enough and better than nothing (wish I could see the name
 of all the devices like the router itself does).

```
sudo apt install arp-scan
sudo arp-scan --localnet
```